name: middleware
group: chain
description: It a provides a way to create, extend and invoke arbitrary middleware chains
acceptanceCriteria:
  - A portable chain object can be created with a basic provider
test: |
  vendor/bin/phpunit --group middleware
